{
  "$schema": "https://design-tokens.org/schema.json",
  "z-index": {
    "z0":  { "$value": 0,    "$type": "number", "$description": "Page surface. No shadow." },
    "z1":  { "$value": 10,   "$type": "number", "$description": "Cards, elevated surfaces. 1px border, no shadow in dark mode." },
    "z2":  { "$value": 100,  "$type": "number", "$description": "Dropdowns, popovers. Subtle shadow + 1px border." },
    "z3":  { "$value": 1000, "$type": "number", "$description": "Modals, overlays. Backdrop blur 12px + scrim rgba(10,10,11,0.6)." },
    "z4":  { "$value": 10000,"$type": "number", "$description": "Toasts, notifications. Voltage left border (3px), no shadow." },
    "max": { "$value": 99999,"$type": "number", "$description": "Reserved for emergency overrides only." }
  }
}
